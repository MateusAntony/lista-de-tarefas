generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Tasks{
  id String @id @default(uuid())
  task String
  done Boolean

}